<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aloha.minihome.mapper.UserMapper">

    <resultMap id="userMap" type="Users">
        <id     property="id"           column="id" />
        <result property="username"     column="username" />
        <result property="password"     column="password" />
        <result property="nickname"     column="nickname" />
        <result property="bio"          column="bio" />
        <result property="profileImage" column="profile_image" />
        <result property="birthDate"    column="birth_date" />
        <result property="email"        column="email" />
        <result property="createdAt"    column="created_at" />
        <result property="updatedAt"    column="updated_at" />

        <collection property="authList"
                  select="authList"
                  column="username"
                  ofType="UserAuth"></collection>
    </resultMap>

    <resultMap id="authMap" type="UserAuth">
        <id     property="id"       column="id" />
        <result property="username" column="username" />
        <result property="auth"     column="auth" />
    </resultMap>

    <!-- 회원가입 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, nickname, bio, profile_image, birth_date, email)
        VALUES (#{username}, #{password}, #{nickname}, #{bio}, #{profileImage}, #{birthDate}, #{email})
    </insert>

    <!-- 권한 등록 -->
    <insert id="insertAuth">
        INSERT INTO user_auth ( username, auth )
        VALUES ( #{username}, #{auth} )
    </insert>

    <!-- username으로 조회 (로그인, 중복체크) -->
    <select id="select" resultMap="userMap">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <!-- id로 조회 (프로필 조회) -->
    <select id="selectById" resultMap="userMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <!-- 권한 목록 조회 -->
    <select id="authList" resultMap="authMap">
        SELECT *
        FROM user_auth
        WHERE username = #{username}
    </select>    

    <!-- 프로필 수정 -->
    <update id="update">
        UPDATE users
            <set>
                <if test="nickname != null">nickname = #{nickname},</if>
                <if test="password != null">password = #{password},</if>
                <if test="bio != null">bio = #{bio},</if>
                <if test="profileImage != null">profile_image = #{profileImage},</if>
                <if test="birthDate != null">birth_date = #{birthDate},</if>
                <if test="email != null">email = #{email},</if>
                updated_at = now()
            </set>
        WHERE username = #{username}
    </update>

    <!-- [개선] 회원 권한 삭제 (cascade) -->
    <delete id="deleteAuth">
        DELETE FROM user_auth
        WHERE username = #{username}
    </delete>

    <!-- 회원 탈퇴 -->
    <delete id="delete">
        DELETE FROM users WHERE username = #{username}
    </delete>

</mapper>
